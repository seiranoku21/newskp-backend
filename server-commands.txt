# Commands yang perlu dijalankan di server (sudah dijalankan):

✅ php artisan optimize
✅ php artisan cache:clear  
✅ php artisan config:clear
✅ head -n 20 AuthController.php | grep "Google" (verified file sudah benar)

# Command yang HARUS dijalankan berikutnya:

composer dump-autoload

# Setelah itu test di Postman lagi

# Optional (jika masih error):
php artisan route:clear
php artisan view:clear

# Restart PHP-FPM (jika diperlukan, tergantung setup server):
# sudo systemctl restart php8.1-fpm
# atau
# sudo service php-fpm restart

